export type ChartConfig = {
  barChartData: {
    labels: string[];
    datasets: any;
  };
  barChartOptions: object;
  doughnutChartData: {
    labels: string[];
    datasets: any;
  };
  doughnutChartOptions: object;
};
